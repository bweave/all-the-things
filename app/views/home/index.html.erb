<%= link_to(
	"Camaros",
	graphql_query_path(query: "{
		classic_camaros{
			title,
			price,
			thumbnail,
			description,
			specs,
			link,
		},
	}"),
	class: "btn btn-primary btn-lg",
) %>

<%= link_to(
	"Mustangs",
	graphql_query_path(query: "{
		classic_mustangs{
			title,
			price,
			thumbnail,
			description,
			specs,
			link
		}
	}"),
	class: "btn btn-primary btn-lg",
) %>